apiVersion: kargo.akuity.io/v1alpha1
kind: Stage
metadata:
  name: stage
  namespace: hello
  annotations:
    kargo.akuity.io/color: "#FFEB3B"
spec:
  requestedFreight:
    - origin:
        kind: Warehouse
        name: hello
      sources:
        stages:
          - dev
  promotionTemplate:
    spec:
      steps:
        - uses: git-clone
          config:
            repoURL: git@github.com:ondrejsikax/kcd2026-kargo-test.git
            checkout:
              - branch: master
                path: ./repo
        - uses: yaml-update
          config:
            path: ./repo/clusters/kargo2/hello/values/stage/env.values.yaml
            updates:
              - key: image
                value: ${{ imageFrom("ttl.sh/ondrejsikax/kcd2026-kargo-test").RepoURL + ":" + imageFrom("ttl.sh/ondrejsikax/kcd2026-kargo-test").Tag }}
        - uses: git-commit
          config:
            path: ./repo
            message: 'chore(clusters/kargo2/hello/values): promote hello:${{ imageFrom("ttl.sh/ondrejsikax/kcd2026-kargo-test").Tag }} image to stage'
            author:
              name: SikaLabs[bot]
              email: sikalabsbot@sikalabs.com
        - uses: copy
          config:
            inPath: ./repo/clusters/kargo2/hello/values/dev/common.values.yaml
            outPath: ./repo/clusters/kargo2/hello/values/stage/common.values.yaml
        - uses: git-commit
          config:
            path: ./repo
            message: 'chore(clusters/kargo2/hello/values): promote hello values to stage'
            author:
              name: SikaLabs[bot]
              email: sikalabsbot@sikalabs.com
        - uses: git-push
          config:
            path: ./repo
        - uses: argocd-update
          config:
            apps:
              - name: hello-stage
