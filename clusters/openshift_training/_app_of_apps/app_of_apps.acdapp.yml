apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: app-of-apps-openshift-taining
  namespace: openshift-gitops
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: app-of-apps
  destination:
    namespace: openshift-gitops
    server: https://kubernetes.default.svc
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - SkipDryRunOnMissingResource=true
  source:
    path: clusters/openshift_training
    repoURL: https://github.com/ondrejsika/argocd-apps-public.git
    directory:
      recurse: true
      exclude: '{**/manifests/**,**/Chart.yaml,**/values.y*ml,**/*.values.y*ml,**/templates/**,**/values/**}'
