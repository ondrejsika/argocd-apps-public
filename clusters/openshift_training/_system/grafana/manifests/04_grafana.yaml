apiVersion: grafana.integreatly.org/v1beta1
kind: Grafana
metadata:
  name: grafana
  namespace: grafana
spec:
  ingress:
    enabled: false
  route:
    spec:
      to:
        kind: Service
        name: grafana-service
      tls:
        termination: edge
