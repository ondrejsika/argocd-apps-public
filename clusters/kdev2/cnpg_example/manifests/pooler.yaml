apiVersion: postgresql.cnpg.io/v1
kind: Pooler
metadata:
  name: pg-pgbouncer
  namespace: cnpg-example
spec:
  cluster:
    name: pg
  instances: 1
  type: ro
  pgbouncer:
    poolMode: transaction
    parameters:
      max_client_conn: "100"
      default_pool_size: "25"
