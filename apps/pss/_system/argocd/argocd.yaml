apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: argocd
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: system
  destination:
    namespace: argocd
    server: https://kubernetes.default.svc
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
  sources:
    - repoURL: https://argoproj.github.io/argo-helm
      chart: argo-cd
      targetRevision: 8.5.6
      helm:
        valueFiles:
          - $values/apps/pss/_system/argocd/argocd.values.yaml
    - repoURL: https://gitlab.sikalabs.com/examples/pss/pss-argocd-apps.git
      ref: values
    - repoURL: https://gitlab.sikalabs.com/examples/pss/pss-argocd-apps.git
      path: ./apps/pss/_system/argocd/manifests
