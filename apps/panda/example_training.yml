apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: example-training
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  destination:
    namespace: examples
    server: https://kubernetes.default.svc
  project: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
  source:
    repoURL: https://helm.sikalabs.io
    targetRevision: 0.6.0
    chart: hello-world
    helm:
      releaseName: example-training
      values: |
        host: example-training.panda.k8s.sl.zone
        TEXT: "Hello from ArgoCD Training"
