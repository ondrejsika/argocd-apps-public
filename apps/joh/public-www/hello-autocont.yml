apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: www-hello-autocont
  namespace: argocd
spec:
  destination:
    namespace: www
    server: https://kubernetes.default.svc
  project: default
  source:
    repoURL: https://github.com/sikalabs/charts.git
    targetRevision: HEAD
    path: charts/hello-world
    helm:
      releaseName: hello-autocont
      values: |
        host: hello-autocont.joh.k8s.sl.zone
        image: sikalabs/hello-world-server
        replicas: 5
        TEXT: Hello Autocont!
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
    - CreateNamespace=true
