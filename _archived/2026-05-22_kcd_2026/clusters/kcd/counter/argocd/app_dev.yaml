apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: counter-dev
  namespace: argocd
  annotations:
    kargo.akuity.io/authorized-stage: "counter:dev"
  finalizers:
    - resources-finalizer.argocd.argoproj.io/foreground
spec:
  project: default
  destination:
    namespace: counter-dev
    server: https://kubernetes.default.svc
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
  sources:
    - repoURL: git@github.com:ondrejsika/kcd2026-kargo-example.git
      path: charts/counter
      targetRevision: master
      helm:
        valueFiles:
          - $values/clusters/kcd/counter/values/dev/env.values.yaml
          - $values/clusters/kcd/counter/values/dev/common.values.yaml
    - repoURL: git@github.com:ondrejsika/kcd2026-kargo-example.git
      targetRevision: master
      ref: values
